# String+

String+ provides a collection of tools for transforming and manipulating text directly within Aura Text. Perform common string operations without leaving the editor.

## Features

- Convert text to uppercase, lowercase, title case, and camel case
- Trim whitespace and remove duplicate lines
- Sort, reverse, and shuffle lines
- Encode and decode Base64 and URL strings
- Find and replace using regular expressions
